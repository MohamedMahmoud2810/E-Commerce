[build]
  command = "composer install && php artisan config:cache"
  ignore = "git diff --quiet HEAD^ HEAD ."
  publish = "public"

[[redirects]]
  from = "/node_modules/*"
  to = "/404.html"
  status = 404